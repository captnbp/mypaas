%echo Generating a default key
Key-Type: default
Subkey-Type: default
Name-Real: Objectstore Backup Signing
Name-Comment: Signing key
Name-Email: backup_signing@{{tld_hostname }}
Expire-Date: 0
Passphrase: {{ gpg.signing_passphrase }}
%pubring backup_signing.pub
%secring backup_signing.sec
# Do a commit here, so that we can later print "done" :-)
%commit
%echo done
